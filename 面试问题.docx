# 高校图书管理系统 - 面试问题集锦

---

## 一、项目概述

这是一个基于 RuoYi-Vue 框架构建的商业级高校图书管理系统，采用前后端分离架构。

**技术栈**：Spring Boot 2.5.15 + MyBatis + Spring Security + JWT（后端），Vue 2.6 + Element UI 2.15（前端），MySQL 8.0 + Redis（缓存）

**核心功能**：图书管理、借阅管理、罚款管理、读者自助门户、数据看板

---

## 第一部分：项目与架构

### 问题1：请讲一下你的项目分层架构，以及为什么要这样分层？

**回答：**

项目采用经典的三层架构（Controller-Service-Mapper），在 RuoYi 框架基础上做了模块化拆分：

1. **ruoyi-admin**：Controller 层，负责接收请求、参数校验、权限控制、响应包装
2. **ruoyi-system**：Service + Mapper 层，负责核心业务逻辑、数据库操作
3. **ruoyi-framework**：框架层，包含安全配置、AOP、过滤器、拦截器
4. **ruoyi-common**：通用工具、实体类、常量定义、异常处理

**为什么这样分层：**

- **单一职责**：每层只做一件事，代码结构清晰
- **便于维护**：修改业务逻辑不影响 Controller，修改 Controller 不影响 Mapper
- **复用性强**：Service 层可以被多个 Controller 调用
- **便于测试**：可以独立对每层进行单元测试

---

### 问题2：为什么选择 RuoYi 框架，而不是从零开发？

**回答：**

1. **节省开发成本**：RuoYi 已经内置了用户管理、角色权限、菜单管理、代码生成等通用功能，可以直接复用
2. **规范统一**：团队基于同一个框架开发，代码风格统一，便于交接
3. **社区成熟**：RuoYi 有广泛的用户群体，遇到问题容易找到解决方案
4. **可扩展性**：框架的模块化设计，便于我们在其基础上扩展图书管理业务

**我们在 RuoYi 基础上主要做的扩展：**

- 新增了图书、读者、借阅、罚款等业务表
- 开发了对应的 Controller、Service、Mapper
- 开发了前端管理页面和读者自助门户
- 集成 Redis 缓存优化性能

---

### 问题3：你的项目中哪里用到了 Redis？用了什么数据结构？为什么这么选择？

**回答：**

Redis 在项目中主要用于热点数据缓存，具体使用场景和数据结构：

**1. 热门图书 TOP10 缓存（String 结构）**

- 代码位置：DashboardController.java#L68-L77
- 使用 String 结构存储 JSON 格式的热门图书列表，设置 30 分钟过期时间
- 原因：TOP10 访问频率高，但变化不频繁，缓存可以显著降低数据库压力

**2. 系统配置缓存（Hash 结构）**

- 借阅规则、罚款规则等配置项，用 Hash 结构存储，便于单条读取和更新

**3. JWT Token 存储（String 结构）**

- 用户登录成功后，Token 存入 Redis，用于后续请求验证和登出时作废

**除了这些，我们还可以用 Redis 做接口限流（滑动窗口或令牌桶算法）、防止重复提交等**

---

### 问题4：你们项目中有分布式事务的问题吗？如果有，怎么解决的？

**回答：**

在当前规模下，我们是单数据源（MySQL），没有跨库的分布式事务问题。但在单库中，我们通过 @Transactional 注解保证了本地事务的一致性。

以借阅流程为例（BorrowServiceImpl.java#L67-L121）：

- 校验读者状态
- 校验借阅数量
- 校验库存
- 插入借阅记录

这些操作都在同一个事务中，要么全部成功，要么全部回滚。

**如果未来扩展到微服务架构（比如用户服务、图书服务、支付服务分离），可能会引入分布式事务，到时可以考虑：**

- Seata（AT 模式）
- 消息队列 + 最终一致性
- TCC 模式

---

## 第二部分：数据库设计

### 问题5：你的借阅记录表（tb_borrow_record）建了哪些索引？为什么这么建？

**回答：**

借阅记录表建了三个索引（参考 book-manager.sql#L196-L198）：

**1. idx_reader_id：读者 ID 索引**

- 查询场景："我的借阅"页面，根据读者 ID 查询该读者的所有借阅记录
- 如果没有索引，会全表扫描，效率极低

**2. idx_book_id：图书 ID 索引**

- 查询场景：某本图书的借阅历史、还书时根据图书找借阅记录

**3. idx_status：状态索引**

- 查询场景：统计当前借阅中、逾期未还的数量

**索引不是越多越好，太多索引会影响写入性能（INSERT/UPDATE/DELETE 时需要更新索引）。我们只给高频查询字段加了索引。**

---

### 问题6：为什么要建配置表（tb_config）？如果让你设计，你会怎么优化？

**回答：**

**配置表的价值：**

1. **规则可配置**：借阅期限、最大借阅数、罚款金额等，不需要改代码就能调整
2. **动态生效**：管理员在后台修改配置，立即生效
3. **多环境适配**：开发、测试、生产环境可以有不同的配置

**如果让我优化：**

1. 增加配置分组：把借阅、罚款、支付等配置分组管理
2. 增加配置描述字段：说明每个配置的作用、取值范围
3. 增加版本号或修改时间：便于追踪配置变更
4. 配合 Redis 缓存：读取配置先查缓存，缓存未命中再查库，修改配置时刷新缓存
5. 增加配置校验：保存配置时校验取值合法性（比如借阅天数不能为负数）

---

### 问题7：你的图书库存是怎么管理的？并发场景下会超卖吗？怎么解决？

**回答：**

在当前实现中，我们的图书表有 stock_count 字段，但借阅流程主要是通过查询借阅记录表来判断图书是否已被借出（BorrowServiceImpl.java#L99-L103），这种方式在并发高的情况下可能出现问题。

**潜在问题：**

如果两个用户同时借同一本书，都查询到"未被借出"，然后都插入借阅记录，就会出现同一本书被借出两次的情况。

**解决方案：**

1. **数据库层面**：加乐观锁（版本号）或悲观锁

```
UPDATE tb_book SET stock_count = stock_count - 1 WHERE book_id = ? AND stock_count > 0
```

根据影响行数判断是否成功

2. **Redis 分布式锁**：
   - 借书前先获取该图书的分布式锁，获取成功才能继续

3. **唯一约束**：
   - 在借阅记录表上建 (book_id, status) 联合唯一索引，状态为"借阅中"的同一本书只能有一条记录

**这些优化我在毕业设计中还没完全实现，但我知道这些方案。**

---

## 第三部分：核心业务

### 问题8：你的逾期罚款是怎么计算的？为什么用 BigDecimal 而不用 double/float？

**回答：**

逾期罚款计算逻辑（BorrowServiceImpl.java#L148-L162）：

1. 还书时比较实际归还日期和应还日期
2. 如果逾期，计算逾期天数 = (实际归还时间 - 应还时间) / (24 * 60 * 60 * 1000)
3. 罚款金额 = 逾期天数 × 每日罚款金额（从配置表读取）

**为什么用 BigDecimal：**

float 和 double 是浮点数，会有精度丢失问题。比如 0.1 + 0.2 = 0.30000000000000004，在金额计算中这是不能接受的。

**BigDecimal 可以保证精确的十进制运算，所以涉及金额的地方都必须用 BigDecimal。**

---

### 问题9：你在借书时做了哪些校验？有没有遗漏的？

**回答：**

借书时的校验（BorrowServiceImpl.java#L67-L121）：

1. 校验读者状态：是否有逾期未还的图书
2. 校验借阅数量：是否达到最大借阅上限
3. 校验库存：图书是否已被借出

**可以补充的校验：**

1. 读者状态是否正常（是否被拉黑、是否已离校）
2. 图书状态是否正常（是否已下架、是否在馆）
3. 该读者是否已经借过同一本书（有些馆不允许同一读者重复借同一本书）
4. 是否有未缴纳的罚款（有些馆要求先缴清罚款才能继续借书）
5. 参数校验：读者 ID、图书 ID 是否存在且有效

---

### 问题10：如果要做"逾期自动扣款"功能，你会怎么设计？

**回答：**

**方案设计：**

**1. 定时任务：**

- 使用 Spring Task 或 XXL-Job，每天凌晨扫描借阅记录表
- 查找 status = "borrowing" 且 due_date < 当前日期 的记录
- 计算罚款金额，更新到 fine_amount 字段

**2. 支付对接：**

- 对接微信/支付宝，发起代扣（需要用户授权）
- 支付成功后更新罚款记录状态、记录支付订单

**3. 消息通知：**

- 逾期前 3 天提醒、逾期当天通知、扣款成功通知
- 方式：站内信、短信、微信公众号通知

**4. 补偿机制：**

- 支付失败重试、定时任务幂等性（避免重复扣款）、人工兜底

---

## 第四部分：性能与安全

### 问题11：Spring Security + JWT 是怎么工作的？Token 存在哪里？怎么续期？

**回答：**

**认证流程：**

1. **用户登录**：验证用户名密码 → 生成 JWT Token → Token 存入 Redis
2. **用户请求**：请求头携带 Token → 过滤器从 Redis 验证 Token 有效性 → 取出用户信息 → 放行
3. **用户登出**：从 Redis 删除 Token

**Token 存储：**

Redis 中，Key 可以是 "token:" + userId，Value 是 JWT Token，过期时间 30 分钟（application.yml#L98）

**Token 续期：**

- **方案1**：每次请求刷新 Redis 过期时间（如果 Token 还有 10 分钟过期，就重新设置为 30 分钟）
- **方案2**：双 Token 机制（Access Token + Refresh Token），Access Token 过期后用 Refresh Token 换取新的 Access Token

**JWT 本身是无状态的，但我们配合 Redis 使用，主要是为了能主动让 Token 失效（比如用户改密码、强制下线）**

---

### 问题12：你怎么防止 SQL 注入？XSS 攻击？CSRF 攻击？

**回答：**

**SQL 注入防护：**

1. 使用 MyBatis 的 #{} 参数绑定（预编译），不要用 ${} 字符串拼接
2. 如果必须用 ${}（比如动态表名），要做白名单校验

**XSS 攻击防护：**

1. 前端：使用 v-text 而不是 v-html，或者对富文本做 XSS 过滤
2. 后端：RuoYi 框架内置了 XSS 过滤器（application.yml#L122-L129），对请求参数做过滤

**CSRF 攻击防护：**

1. Spring Security 可以开启 CSRF 保护（但前后端分离项目通常会关闭 CSRF，改用 Token 机制）
2. 我们用的 JWT Token 本身就可以防 CSRF（因为 CSRF 攻击无法获取 Token）
3. 配合 SameSite Cookie 属性

---

### 问题13：你的项目有做接口限流吗？怎么实现的？

**回答：**

目前项目中没有完整的限流实现，但我知道常见的实现方案：

**方案1：Redis + Lua 脚本（滑动窗口或令牌桶）**

- Lua 脚本可以原子性执行"计数 + 判断是否超过阈值"操作

**方案2：Spring Cloud Gateway + Redis 限流器（如果是微服务架构）**

**方案3：使用 Google Guava 的 RateLimiter（单机版）**

**如果要在本项目中加限流：**

- 在 ruoyi-framework 中定义一个限流注解 @RateLimiter
- 定义切面，拦截带注解的方法
- 使用 Redis 记录调用次数
- 超过阈值返回 429 Too Many Requests

---

## 第五部分：扩展与优化

### 问题14：如果要做图书全文检索，你会怎么实现？

**回答：**

**方案对比：**

**1. 数据库 LIKE 查询（最 low）：**

- 缺点：无法分词、无法排序、索引失效（%keyword%）、性能差

**2. MySQL 全文索引：**

- 缺点：中文分词效果不好、功能有限

**3. Elasticsearch（推荐）：**

- **优点**：支持中文分词（IK 分词器）、支持模糊搜索、支持相关性排序、性能好
- **实现思路**：
  - a. 建立图书索引（book_index）
  - b. 新增/更新图书时同步到 ES
  - c. 搜索时从 ES 查询

**4. 还可以考虑接入 OpenSearch（阿里云）或 Solr**

---

### 问题15：借阅记录表数据量大了怎么办？

**回答：**

数据量大了之后的优化方案：

**1. 归档历史数据：**

- 把已归还超过 1 年的记录归档到另一张表（tb_borrow_record_history）
- 或者归档到历史库，主表只保留近期数据

**2. 分表：**

- 按时间分表（按月或按年），比如 tb_borrow_record_2024、tb_borrow_record_2025
- 或者按读者 ID 哈希分表

**3. 读写分离：**

- 主库负责写，从库负责读

**4. 增加缓存：**

- "我的借阅"页面的记录缓存到 Redis

**5. 限制查询范围：**

- 默认只查最近 3 个月的记录，查更早的需要手动选择时间范围

---

### 问题16：你在项目中遇到过什么最难的问题？怎么解决的？

**回答：**

（可以根据实际情况说，这里给一个通用的例子）

**我遇到过的一个问题：数据看板加载慢**

- **现象**：仪表盘页面打开需要 3-5 秒，用户体验不好
- **原因**：每个统计都在实时查库，其中热门图书 TOP10 的查询最耗时（需要关联借阅记录和图书表，按借阅次数排序）

**解决过程：**

1. **加索引**：给借阅记录表的 book_id 和 status 加索引
2. **SQL 优化**：减少子查询，优化 JOIN
3. **Redis 缓存**：把热门图书 TOP10 缓存 30 分钟（DashboardController.java#L68-L77）
4. **异步统计**：每天凌晨预先生成前一天的统计数据，存入统计表

**结果**：页面加载时间降到 1 秒以内

**收获**：性能优化不是靠加缓存就能解决所有问题，要先定位瓶颈（用 EXPLAIN 分析 SQL、用 Arthas 分析方法耗时），然后针对性优化

---

### 问题17：如果让你把这个项目改造成微服务，你会怎么拆分？

**回答：**

**微服务拆分思路：**

**1. 用户中心服务（user-service）：**

- 负责：用户管理、角色权限、认证授权、SSO

**2. 图书中心服务（book-service）：**

- 负责：图书管理、分类管理、库存管理

**3. 借阅中心服务（borrow-service）：**

- 负责：借书、还书、续借、借阅记录查询

**4. 支付中心服务（payment-service）：**

- 负责：罚款管理、支付订单、对接微信/支付宝

**5. 消息中心服务（message-service）：**

- 负责：站内信、短信、邮件通知

**技术选型：**

- Spring Cloud Alibaba（Nacos + Sentinel + Seata + RocketMQ）
- 或者 Spring Cloud Netflix（Eureka + Hystrix + Ribbon + Zuul）

---

## 第六部分：个人相关

### 问题18：你在这个项目中主要负责哪部分？

**回答：**

（根据实际情况说，这里给一个参考）

**我主要负责借阅管理模块和读者自助门户：**

**后端部分：**

- 设计了借阅记录表、罚款记录表、支付订单表
- 开发了借书、还书、续借的业务逻辑（BorrowServiceImpl.java）
- 开发了借阅管理的 Controller
- 实现了逾期罚款计算

**前端部分：**

- 开发了借阅管理页面（借书、还书、续借操作）
- 开发了读者自助门户（我的借阅、图书检索、我的罚款）
- 开发了数据看板的部分图表（ECharts 实现）

**其他：**

- 配合做了 Redis 缓存优化
- 编写了部分单元测试

---

### 问题19：你觉得你的项目有什么不足？接下来想怎么优化？

**回答：**

**项目的不足：**

1. 没有做接口限流，防止恶意刷接口
2. 没有做分布式锁，高并发借书可能出现超卖
3. 没有完整的支付功能，只是设计了表结构
4. 没有消息通知功能，逾期提醒、还书提醒都没有
5. 没有操作日志审计，谁在什么时候借了哪本书，记录不够详细
6. 单元测试覆盖不够

**接下来想优化的方向：**

1. 引入 Redis 分布式锁，解决并发问题
2. 实现接口限流
3. 接入微信支付，完成罚款在线缴纳
4. 引入消息队列，实现逾期提醒、异步处理
5. 增加操作日志，记录关键操作
6. 补充单元测试和集成测试

---

### 问题20：如果我们公司不用你用的这些技术（比如不用 Spring Boot 用 Go，不用 Vue 用 React），你怎么办？

**回答：**

我觉得技术只是工具，核心是编程思想和架构设计能力。

虽然我现在用的是 Spring Boot + Vue，但我理解的是：

- MVC 分层思想
- RESTful API 设计
- 数据库设计规范
- 缓存、消息队列这些中间件的使用场景

**这些思想是通用的，换一门语言只是语法不一样而已。**

而且我学习能力很强，我可以快速上手新的技术栈。比如之前我没学过 Redis，为了做缓存优化，我花了一周时间就把 Redis 的常用数据结构和使用场景都搞懂了，还用到了项目里。

**所以如果公司用其他技术，我可以快速学习，尽快融入团队。**


